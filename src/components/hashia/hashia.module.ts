import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { Hashia } from './hashia';

@NgModule({
  declarations: [
    // Hashia,
  ],
  imports: [
    // IonicPageModule.forChild(Hashia),
  ],
  exports: [
    // Hashia
  ]
})
export class HashiaModule {}
